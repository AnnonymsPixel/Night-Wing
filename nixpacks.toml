FROM python:3.11-slim

RUN apt-get update && apt-get install -y ffmpeg && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

**Then in Railway:**
1. Go to your service → **Settings**
2. Find **Builder** → change it to **Dockerfile**
3. Make sure the Dockerfile path is set to `Dockerfile`
4. Redeploy

---

If you don't have a `requirements.txt` yet, create one in your project root:
```
discord.py
yt-dlp
python-dotenv
PyNaCl
